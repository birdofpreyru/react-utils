import Beware from '../common/_beware-depends-on-app-setup.md';

# getBuildTimestamp()

```tsx
import { getBuildTimestamp } from '@dr.pogodin/react-utils';

function getBuildTimestamp(): string;
```
Returns a **string** &mdash; the build timestamp of the frontend JS bundle in
ISO format.

<Beware />
