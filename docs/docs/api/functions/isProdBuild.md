import Beware from '../common/_beware-depends-on-app-setup.md';

# isProdBuild()

```tsx
import { isProdBuild } from '@dr.pogodin/react-utils';

function isProdBuild(): boolean;
```

Returns **boolean** &mdash; **true** if the production version of code is
running, **false** otherwise.

<Beware />
