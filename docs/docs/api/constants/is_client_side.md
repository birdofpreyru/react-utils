import Beware from '../common/_beware-depends-on-app-setup.md';
import Info from './_info-is-client-server-side.md';

# IS_CLIENT_SIDE
```tsx
import { IS_CLIENT_SIDE } from '@dr.pogodin/react-utils';
```

Equals **true** within the client-side (browser) environment,
and equals **false** at the server-side (NodeJS).

<Beware />
<Info />
