import Beware from '../common/_beware-depends-on-app-setup.md';
import Info from './_info-is-client-server-side.md';

# IS_SERVER_SIDE
```tsx
import { IS_SERVER_SIDE } from '@dr.pogodin/react-utils';
```

Equals **true** within the server-side (NodeJS) environment,
and equals **false** at the client-side

<Beware />
<Info />
